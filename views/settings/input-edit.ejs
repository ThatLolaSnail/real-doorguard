<%- include('../template.ejs',{
    mode:"header",
    onload: "onLoad(" + Object.values(types).indexOf(input.type) + ")"
}); %>

<script>
    function onLoad(selectedIndex){
        document.getElementById('type').selectedIndex = selectedIndex;

        onChange();
    }

    function onChange() {
        const hardware_pin = document.getElementById('hardware_pin');
        const discord_channel = document.getElementById('discord_channel');
        const discord_message = document.getElementById('discord_message');
        hardware_pin.style.display = "none";
        discord_channel.style.display = "none";
        discord_message.style.display = "none";
        const type = document.getElementById('type');
        if (type.value === "hardware"){
            hardware_pin.style.display = "";
        } else if (type.value === "discord"){
            discord_channel.style.display = "";
            discord_message.style.display = "";
        }
    }
</script>

<h1>Settings: Edit a Input</h1>

<%- include('settings-template.ejs'); %>

<p>
    A Input is everything between a button press and a bell ring, so all the settings.<br>
    Don't forget to save after editing changes.<br>
    To disable the condition upper limit, set it to 0.
</p>

<form action="/settings/input/edit" method="post">
    <input type="hidden" id="id" name="id" value="<%= input.id %>">
    <table>
        <tr>
            <th>Id</th>
            <th><%= input.id %></th>
        </tr>
        <tr>
            <th><label for="name">Name</label></th>
            <th><input type="text" id="name" name="name" value="<%= input.name %>"></th>
        </tr>
        <tr>
            <th><label for="description">Description</label></th>
            <th><textarea id="description" name="description" rows="2" cols="40"><%= input.description %></textarea></th>
        </tr>
        <tr>
            <th><label for="type">Type</label></th>
            <th>
                <select id="type" name="type" onchange=onChange()>
                    <% for (let t of Object.values(types)) { %>
                        <option value="<%= t %>"><%= t %></option>
                    <% } %>
                </select>
            </th>
        </tr>
        <tr id="hardware_pin">
            <th><label for="pin">Output pin:</label></th>
            <th><input type="number" id="pin" name="pin" value="<%= input.pin %>"></th>
        </tr>
        <tr id="discord_channel">
            <th><label for="channel">Channel</label></th>
            <th><input type="text" id="channel" name="channel" value="<%= input.channel %>"></th>
        </tr>
        <tr id="discord_message">
            <th><label for="message">Message</label></th>
            <th><input type="text" id="message" name="message" value="<%= input.message %>"></th>
        </tr>
        <tr>
            <th><label for="timeFrom">Time (from</label>/<label for="timeTo">to)</label></th>
            <th><input type="time" id="timeFrom" name="timeFrom" value="<%= input.timeFrom.toString() %>"> -
                <input type="time" id="timeTo" name="timeTo" value="<%= input.timeTo.toString() %>"></th>
        </tr>
        <tr>
            <th><label for="enabled">Enabled</label></th>
            <th><input type="checkbox" id="enabled" name="enabled" value="enabled" <%= input.enabled ? 'checked="true"' : ""%>></th>
        </tr>

    </table>

    <p>
        <input type="submit" value="Submit">
        <a href="/settings/input"> Cancel </a>
    </p>
</form>

<%- include('../template.ejs',{mode:"footer"}); %>
